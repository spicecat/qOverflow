import { useState } from 'react'
import { CommentsList } from '../components';

export default function CommentsListController({ }) {
    // const [comments, setComments]
    // useEffect(() => {
    //     selectQuestion(question_id);
    // }, []);

    // return <Question {...questionData} />;
}
