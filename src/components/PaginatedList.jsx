import { List, ListItemText, Pagination, Typography, Button } from '@mui/material'

export default function PaginatedList({ name, data, Component }) {
    return (
        <Pagination>
            <List>

            </List>
        </Pagination>
    );
}